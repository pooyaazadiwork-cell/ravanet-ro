# اگر فایل‌های کامل‌تر لازم شد

ریپو الان اسکلت و هستهٔ رو را دارد.

بستهٔ کامل اسناد + کد: از چت گراک فایل `RO_SOURCE_BOX.zip` را از صفحهٔ ریپو با Upload file هم می‌توانی اضافه کنی.

اجرا:
```bash
cd ro-live/server && node server.mjs
```
