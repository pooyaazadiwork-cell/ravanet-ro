# بازسازی کامل کد ro-live

## روش ۱ — دانلود از Release / زیپ چت
فایل `RO_SOURCE_BOX.zip` را از گفتگو یا Upload دستی به ریپو اضافه کن و از حالت فشرده خارج کن.

## روش ۲ — اجرای مستقیم (وقتی فایل‌های server و js کامل باشند)
```bash
cd ro-live/server
node server.mjs
```

http://127.0.0.1:8787

## وضعیت آپلود
اسکلت UI + safety + prompt + بخشی از ماژول‌ها روی main هستند.
برای نسخهٔ کامل یکجا، `RO_SOURCE_BOX.zip` را با Upload files به همین ریپو اضافه کن.
