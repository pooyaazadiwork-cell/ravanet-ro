# RAG رو

- **46** قطعه دانش عملیاتی
- فایل یکجا: `chunks.json` (در سورس‌باکس کامل)
- یا چندفایلی: `chunks_policy.json`, `chunks_safety.json`, `chunks_clinical.json`, `chunks_skills.json`, `chunks_product.json`
- بازیابی کلیدواژه‌ای در `retrieve.mjs`
- قبل از LLM به کانتکست تزریق می‌شود

نه تشخیص، نه دارو، نه جایگزین درمانگر.
